UNAME_S := $(shell uname -s)
ifeq ($(UNAME_S),Linux)
    PKG_CXXFLAGS = -march=native 
endif
ifeq ($(UNAME_S),Darwin)
    UNAME_P := $(shell uname -p)
    ifeq ($(UNAME_P), i386)
        PKG_CXXFLAGS = -march=native 
    endif 
endif
